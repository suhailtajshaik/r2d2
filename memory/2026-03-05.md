# 2026-03-05

## Skills Setup
- Confirmed server-health, screenshot, firecrawl skills already installed and ready
- Firecrawl needs FIRECRAWL_API_KEY — Suhail hasn't provided yet
- Skipped Gmail and Calendar setup per Suhail's request

## Agents Framework
- Set up `~/projects/agents/` as the standalone agents workspace
- Stack: Claude (anthropic SDK) + LangGraph + Pydantic + Rich
- Pattern: state.py → nodes.py → graph.py → agent.py (CLI)
- Each agent gets its own folder with venv, .env, requirements.txt

## Review Agent Built
- Location: `~/projects/agents/review-agent/`
- Pattern: Evaluator-Optimizer (from Anthropic's "Building Effective Agents")
- Flow: Code → Reviewer (Claude) → Evaluator (Claude) → loop if score < 7 → Finalize
- LangGraph state machine with conditional edges
- CLI supports: file review, git diff, stdin pipe, JSON output
- Deps installed in .venv (anthropic, langgraph, langchain-anthropic, rich, etc.)
- Needs ANTHROPIC_API_KEY in .env to run
- Graph compiles and CLI works ✅
